# 🧠 SonqoBase - AI Context & Operations

Este archivo configura el contexto para el desarrollo asistido por IA en SonqoBase.

## 📍 Contexto del Proyecto
*   **Nombre:** SonqoBase API
*   **Misión:** Base de Datos Vectorial Efímera as-a-Service (RAG fácil).
*   **Arquitectura:** Layered (API -> Service -> Infra). Ver `.agent/CONVENTIONS.md`.
*   **Estado:** Producción (MVP).

## 🛠️ Comandos Slash (Workflows)

Usa estos comandos para iniciar flujos de trabajo estandarizados:

### Core Workflows
*   `/new-feature` -> Crea feature completa con Git flow automatizado (branch, scaffold, commit)
*   `/create-pr` -> Crea PR en GitHub con validación completa y descripción automática
*   `/arch-check` -> Verifica que no se violen las reglas de capas (imports prohibidos)
*   `/lint` -> Valida estilo de código y aplica auto-fixes (imports, formateo, type hints)
*   `/audit-rag` -> Revisa el flujo de RAG query y optimización

### Executive Workflows
*   `/plan-sprint` -> COO planifica próximo sprint basado en prioridades
*   `/tech-review` -> CTO evalúa decisiones técnicas importantes
*   `/growth-review` -> CMO analiza métricas de crecimiento
*   `/quarterly-review` -> CEO revisa OKRs y ajusta estrategia

## 🚨 Reglas de Comportamiento (System Prompts)

1.  **Strict Layering:** Si el usuario pide un endpoint, NUNCA escribas lógica en el controlador. Crea un `Service`.
2.  **No Hallucinations:** Si no conoces una librería, usa la estándar (`pymongo`, `requests`, `httpx`).
3.  **Security First:** Siempre valida ownership de recursos (`user_id` en queries).
4.  **Performance:** Cuidado con `Sync` en rutas `Async`.
5.  **Imports en la Raíz:** NUNCA pongas imports dentro de funciones/clases. Ver `.agent/CODE_STYLE.md`.
6.  **Commits Firmados:** Todos los commits autogenerados deben incluir firma de Antigravity.

## 📂 Mapa Mental (File Structure)

*   `app/api/` -> Rutas / Controladores
*   `app/services/` -> Lógica de Negocio (Use Cases)
*   `app/infra/` -> DB, AI Clients, Event Bus
*   `app/domain/` -> Modelos puros
*   `app/listeners/` -> Event Handlers
*   `app/templates/` -> Frontend (Jinja2)
*   `app/static/` -> CSS/JS

## 🤖 Subagentes Ejecutivos

Consulta estos agentes para decisiones estratégicas:

*   **COO** (`.agent/agents/coo-agent.md`) -> Priorización, sprint planning, métricas operacionales
*   **CTO** (`.agent/agents/cto-agent.md`) -> Decisiones arquitectónicas, tech debt, performance
*   **CMO** (`.agent/agents/cmo-agent.md`) -> Growth strategy, content marketing, SEO
*   **CEO** (`.agent/agents/ceo-agent.md`) -> OKRs, visión estratégica, risk management

## 🔄 Rutinas Comunes

### Iniciar nueva Feature
1. Ejecuta `/new-feature NOMBRE` (automatiza Git flow + scaffold)
2. Revisa `.agent/CONVENTIONS.md` para arquitectura
3. Implementa de adentro hacia afuera: Infra -> Service -> API
4. Ejecuta `/lint` y `/arch-check` antes de commit
5. Ejecuta `/create-pr` para crear PR con validación

### Commits Autogenerados
Todos los commits de Antigravity usan este formato:
```
feat(scope): description

Body explaining changes

🤖 Auto-generated by Antigravity
Co-authored-by: Claude Sonnet 4 <antigravity@deepmind.google>
```
